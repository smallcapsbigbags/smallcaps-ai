name: retired-aim-daily-visual-audit

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired by company-repository Pass 1
        run: echo "The AIM Daily is no longer a customer product. Historical files remain until the final migration pass."
