name: retired-daily-inclusive-investor-workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired by company-repository Pass 1
        run: echo "The Daily-inclusive investor workflow is retired. News, Watchlist and Company now form the product."
