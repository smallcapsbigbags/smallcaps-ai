name: retired-company-intelligence-audit

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired by company-repository Pass 1
        run: echo "The Company Intelligence audit is retired. Pass 2 introduces a dedicated NOW / CHANGE / WATCH / KEY NUMBERS / NEWS repository audit."
