name: retired-four-surface-launch-readiness

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired by company-repository Pass 1
        run: echo "The previous Daily-led launch journey is retired. Company-repository launch acceptance replaces it."
